export interface TaskList {
    task: string, 
    checked: boolean
}

export interface Tasks {
    taskName: string,
    taskDescription: string, 
    date: Date,
    category: string
}

export interface Data {
    info: string, 
    category: string
}